interface Config {
    baseURL: string
}

export const configyProduction: Config = {
    baseURL: "http://localhost:3000",
}

export const configLocal: Config = {
    baseURL: "http://localhost:3000",
}
